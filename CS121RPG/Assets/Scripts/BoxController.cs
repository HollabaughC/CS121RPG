using UnityEngine;

/*
This script is attached to the Empty Lines in the Drag/Drop Quiz (Alice-like), it just contains a personal boolean to check if it is "holding" a CodeBlock.    
*/
public class BoxController : MonoBehaviour
{
    public bool isHolding = false;
}
